services:
  test:
    build:
      context: .
      args:
        UID: "${DOCKER_UID:-1000}"
        GID: "${DOCKER_GID:-1000}"
    volumes:
      - .:/app
    working_dir: /app
    command: python -m pytest test_suite.py -v
